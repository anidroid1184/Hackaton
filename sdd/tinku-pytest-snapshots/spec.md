# Spec (SDD) — snapshots pytest middleware Tinku

**Fuente de verdad técnica:** [`docs/DEVELOPMENT.md`](../../docs/DEVELOPMENT.md) (entorno, pytest, variables, enlaces a OpenAPI y manifest).

Este archivo solo ancla el cambio en el flujo SDD; no duplicar requisitos aquí — actualizar `docs/DEVELOPMENT.md` y, si aplica, `docs/TINKU_MIDDLEWARE_API.yml` + `backend/tests/fixtures/tinku_endpoints.yaml` en el mismo cambio.

**Memoria entre sesiones:** Engram (`topic_key` acordado con el equipo / orchestrator).
